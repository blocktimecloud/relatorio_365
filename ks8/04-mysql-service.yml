apiVersion: v1
kind: Service
metadata:
  name: mysql
  namespace: office365
spec:
  selector:
    app: mysql
  ports:
    - port: 3306
      targetPort: 3306
  clusterIP: None   # headless service — par natural do StatefulSet